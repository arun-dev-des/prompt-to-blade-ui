import { PromptStudio } from './PromptStudio';

/**
 * LEVEL 2 — Freestyle, Blade-native.
 *
 * A prompt studio: the user describes a screen/component and it is composed live
 * from a library of real Blade recipes (see recipes.tsx). Deterministic and
 * backend-free, fully on-system, and theme-agnostic (works under the dark toggle).
 */
export const Playground = () => <PromptStudio />;
